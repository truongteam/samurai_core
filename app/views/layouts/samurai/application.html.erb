<!DOCTYPE html>
<html class="h-full bg-gray-200" lang="en">
<head>
  <title>Core</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_client_tag %>
  <%= vite_typescript_tag 'layout' %>
</head>
<body class="font-sans leading-none text-gray-800 antialiased">
<div class="flex flex-col h-screen justify-between">
<div class="h-10 mb-10">
<%= render 'layouts/samurai/menu' %>
</div>
<div class="mb-auto h-10">
<div class="container mx-auto">
<%= yield %>
<%= yield(:header) %>
</div>
</div>
<div class="h-10">
<%= render 'layouts/samurai/footer' %>
</div>

</body>
</html>
